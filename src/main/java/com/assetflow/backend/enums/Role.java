package com.assetflow.backend.enums;

/**
 * Represents the role of a user in the system, which determines their permissions.
 */
public enum Role {
    ADMIN,
    ASSET_MANAGER,
    DEPARTMENT_HEAD,
    EMPLOYEE
}
